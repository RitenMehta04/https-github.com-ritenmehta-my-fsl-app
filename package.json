{
  "name": "my-fsl-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "npx eslint . --ext .js --max-warnings=0",
    "test": "jest",
    "build": "echo 'Building...' && exit 0",
    "prepare": "husky"
  },
  "lint-staged": {
    "src/**/*.{js,jsx}": "npx eslint --fix",
    "__tests__/**/*.{js,jsx}": "npx eslint --fix",
    "*.{js,jsx}": "npx eslint --fix",
    "*.{json,md,yml,yaml}": "npx prettier --write"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "lint-staged": "^16.2.6",
    "prettier": "^3.3.3"
  }
}
